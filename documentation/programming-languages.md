I'll directly explain the language usage in your project:

### Language Distribution:

1. **JavaScript (55%)**
   - **Server-side (Node.js)** - 30%
     - `server.js` - Main server setup
     - `routes/compare.js` - API routing
     - `scrapers/amazon.js` and `scrapers/flipkart.js` - Web scraping logic
   - **Frontend** - 25%
     - `public/app.js` - User interface and interactions
     - API calls and data handling
     - DOM manipulation

2. **HTML (25%)**
   - `public/index.html` - Main application page
   - `public/about.html` - About page
   - `public/contact.html` - Contact page
   - `public/how-it-works.html` - Instructions page
   - `public/privacy-policy.html` - Privacy policy

3. **CSS (20%)**
   - `public/css/` directory containing:
     - `common.css` - Shared styles
     - `home.css` - Homepage styles
     - `about.css` - About page styles
     - `contact.css` - Contact page styles
     - `how-it-works.css` - Instructions page styles
     - `privacy-policy.css` - Privacy policy styles

### Main Technologies Used:

1. **Backend:**
   - Node.js for server runtime
   - Express.js for web framework
   - Axios for HTTP requests
   - Cheerio for web scraping

2. **Frontend:**
   - Pure JavaScript (no frameworks)
   - HTML5 for structure
   - CSS3 for styling
   - Responsive design principles

### Key Features by Language:

1. **JavaScript Features:**
   - Async/await for asynchronous operations
   - Promise handling
   - DOM manipulation
   - Event handling
   - Web scraping
   - API routing

2. **HTML Features:**
   - Semantic markup
   - Forms and inputs
   - Responsive design elements
   - Cross-browser compatibility

3. **CSS Features:**
   - Flexbox layouts
   - Grid systems
   - Media queries
   - Animations and transitions
   - Responsive design

This distribution shows that JavaScript is the primary language, handling both backend and frontend functionality, while HTML provides the structure and CSS handles the visual presentation and responsiveness.'
